diff --git a/src/HMRClient.native.ts b/src/HMRClient.native.ts
index 64c164eb332756321e2fe5c1c1974538efe507de..b158a7099fddf6af648ab20eaf2332ac3598168b 100644
--- a/src/HMRClient.native.ts
+++ b/src/HMRClient.native.ts
@@ -1,3 +1,7 @@
-const HMRClient = require('react-native/Libraries/Utilities/HMRClient');
+const HMRClientModule = require('react-native/Libraries/Utilities/HMRClient');
+// PATCH: mesmo problema de interop do `export default` — sem isso, este
+// módulo reexportaria { default: HMRClient } em vez do objeto real,
+// quebrando setupHMR.ts/loadBundle.ts (ex: "HMRClient.setup is not a function").
+const HMRClient = HMRClientModule.default ?? HMRClientModule;
 
 export default HMRClient;
diff --git a/src/messageSocket.native.ts b/src/messageSocket.native.ts
index 1d7c93325d1af908040d6f5b7ccf7e990b040c3c..18ff22cdda9dbbd30490c07c68ee4b086583bd0e 100644
--- a/src/messageSocket.native.ts
+++ b/src/messageSocket.native.ts
@@ -1,7 +1,12 @@
 /* eslint-env browser */
 
 function createWebSocketConnection(path: string = '/message') {
-  const getDevServer = require('react-native/Libraries/Core/Devtools/getDevServer');
+  const getDevServerModule = require('react-native/Libraries/Core/Devtools/getDevServer');
+  // PATCH: getDevServer.js usa `export default`; dependendo do transform de
+  // interop do Metro, o require() acima pode devolver o objeto do módulo
+  // ({ default: fn }) em vez da função direto — daí o erro em runtime
+  // "getDevServer is not a function (it is Object)".
+  const getDevServer = getDevServerModule.default ?? getDevServerModule;
   const devServer = getDevServer();
   if (!devServer.bundleLoadedFromServer) {
     throw new Error('Cannot create devtools websocket connections in embedded environments.');
@@ -9,7 +14,11 @@ function createWebSocketConnection(path: string = '/message') {
 
   const devServerUrl = new URL(devServer.url);
   const serverScheme = devServerUrl.protocol === 'https:' ? 'wss' : 'ws';
-  const WebSocket = require('react-native/Libraries/WebSocket/WebSocket');
+  const WebSocketModule = require('react-native/Libraries/WebSocket/WebSocket');
+  // PATCH: mesmo problema de interop do `export default` acima, agora pra
+  // classe WebSocket — sem isso, "new WebSocket(...)" falha com
+  // "constructor is not callable" (o require devolve { default: WebSocket }).
+  const WebSocket = WebSocketModule.default ?? WebSocketModule;
   return new WebSocket(`${serverScheme}://${devServerUrl.host}${path}`);
 }
 

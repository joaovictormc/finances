# URL da API (apps/api) — em dispositivo físico/Expo Go, troque localhost pelo
# IP da máquina de desenvolvimento na rede local (Expo Go não acessa localhost
# do computador).
EXPO_PUBLIC_API_URL="http://localhost:3001"
